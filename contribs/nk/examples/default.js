/**
 * The default ngeo template based URL, used as it by the template cache.
 * @type {string}
 */
ngeo.baseTemplateUrl = '../../../src/directives/partials';


/**
 * The default nk template based URL, used as it by the template cache.
 * @type {string}
 */
nk.baseTemplateUrl = '../src/directives/partials';
